function sayHello(firstName, lastName){if (!firstName) throw *You must supply the first name parameter*
    if ('lastName) throw *You must supply the last name parameten*
    if (typeof firstName I= "string*) throw 'First Name Must Be A String ;
    if (typeof lastName
    I = 'string') throw Last Name Must Be A String*;
    return "Hello $ffirstName} ${lastName}! How are you?"
}
